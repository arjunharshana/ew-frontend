# The URL of the EW Scheduler Node.js backend
# For local development against a local Docker container: http://localhost:4000
# For EC2 production, change this to the public IP or domain of your EC2 backend
VITE_API_BASE_URL=http://localhost:4000
